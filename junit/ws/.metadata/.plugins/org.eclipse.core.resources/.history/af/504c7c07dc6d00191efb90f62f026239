package calculadora;

import static org.junit.Assert.*;

import org.junit.Test;

public class CalculadoraTest {

	@Test //con esto se hace que se marca el metodo como una prueba
	public void test() {
		/***si se escribe una llamada fail la prueba automaticamente fallará*/
		fail("Not yet implemented");
		/**Si en algun momento se quiere que la prueba falle, ejemplo que se 
		 * este ejecutando un error en el código y se den cuenta que no debería
		 * hacer lo correcto*/
		fail("buuu");
	}

}
